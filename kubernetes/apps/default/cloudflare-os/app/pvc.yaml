---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cloudflare-os-data
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: ceph-bulk
  resources:
    requests:
      storage: 10Gi
