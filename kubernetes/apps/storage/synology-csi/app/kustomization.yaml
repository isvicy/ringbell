---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - externalsecret.yaml
  - storageclass.yaml
  # Synology CSI is deployed via raw manifests from upstream
  # See: https://github.com/SynologyOpenSource/synology-csi
  # TODO: Add Synology CSI manifests after verifying compatibility with Talos
