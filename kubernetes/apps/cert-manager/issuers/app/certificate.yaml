---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ringbell-cc-wildcard
spec:
  secretName: ringbell-cc-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: ringbell.cc
  dnsNames:
    - ringbell.cc
    - "*.ringbell.cc"
