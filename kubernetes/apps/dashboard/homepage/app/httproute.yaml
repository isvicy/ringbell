---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage
spec:
  hostnames:
    - homepage.ringbell.cc
  parentRefs:
    - name: external-auth
      namespace: networking
  rules:
    - backendRefs:
        - name: homepage
          port: 3000
