name: kube-prometheus-stack
namespace: observability
wave: 3
type: helm
adopted: true
repoURL: https://prometheus-community.github.io/helm-charts
chart: kube-prometheus-stack
version: "82.15.1"
resources: true
# grafana chart generates a random admin-password (no existingSecret); preserve it.
ignoreDifferences:
  - kind: Secret
    namespace: observability
    name: kube-prometheus-stack-grafana
    jsonPointers: ["/data"]
