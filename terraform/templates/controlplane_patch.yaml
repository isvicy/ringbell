# controlplane patch (excerpt)
cluster:
  network:
    cni:
      name: none     # install Cilium right after bootstrap
  proxy:
    disabled: true

machine:
  features:
    kubePrism:
      enabled: true
      port: 7445     # default; safe to keep
