{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "filepath=$(jq -r '.tool_input.file_path // empty') && [ -n \"$filepath\" ] && echo \"$filepath\" | grep -qE '\\.ya?ml$' && yamllint -d relaxed \"$filepath\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
